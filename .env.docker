# Docker Container Environment Variables
# Copy this file and rename to .env.docker.local for local development overrides

# Application Configuration
PORT=3000

# Database Configuration (for Docker containers)
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
DATABASE_NAME=stock_price_checker

# External API Configuration
FINNHUB_API_KEY=your_finnhub_api_key_here
STOCK_FETCH_INTERVAL="0 * * * * *"
MAX_CONCURRENT_REQUESTS=5
RATE_LIMIT_DELAY=1000

# Optional: Override default Docker settings
# COMPOSE_PROJECT_NAME=stock-price-checker
